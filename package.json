{
  "name": "broken-pipeline",
  "version": "0.0.1",
  "description": "A test repo to demostrate a broken pipeline",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo \"Dummy build...\" && exit 0",
    "dummy": "echo \"Dummy to trigger run 3\" && exit 0"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "wait-on": "^8.0.0"
  },
  "dependencies": {
    "nodemon": "^3.0.0"
  }
}
